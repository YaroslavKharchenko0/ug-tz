const APP = {
  CONFIG: Symbol('APP_CONFIG'),
}

const MICROSERVICE = {
  CONFIG: Symbol('MICROSERVICE_CONFIG'),
}

const METRICS = {
  SERVICE: Symbol('METRICS_SERVICE'),
}

export const DI = {
  APP,
  MICROSERVICE,
  METRICS,
}
